{
  "name": "studyquest",
  "version": "2.1.0",
  "private": true,
  "description": "StudyQuest — daily class quests, XP, journal, login + cross-device sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "mongodb": "^6.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
