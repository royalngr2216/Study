{
  "name": "studyquest",
  "version": "2.0.0",
  "private": true,
  "description": "StudyQuest — daily class quests, XP, journal, login + cross-device sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
